#go inside anaconda virtual environment(python verion 3.10)
#conda create --name myenv python=3.10
#conda activate ./venv

#pip install -r requirements.txt

#also do pip install llama-index-llms-ollama


#python setup.py install
